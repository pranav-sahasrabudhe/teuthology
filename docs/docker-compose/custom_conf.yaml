#custom
overrides:
  ansible.cephlab:
    branch: containerized-nodes
    skip_tags: "timezone,nagios,monitoring-scripts,ssh,hostname,pubkeys,zap,sudoers,kerberos,selinux,lvm,ntp-client,resolvconf"
    vars:
      containerized_node: true
      ansible_user: root
      cm_user: root